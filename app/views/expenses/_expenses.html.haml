%table.table.table-striped
  %thead
    %th.col-md-1 Date
    %th.col-md-3 Description
    %th.col-md-2 Amount
    %th.col-md-2 Category
    %th.col-md-3 Payment Method
    %th.col-md-1 Actions

  %tbody
    - @expenses.each do |expense|
      %tr
        %td= formatted_date(expense.date)
        %td= expense.description
        %td= number_to_currency(expense.amount)
        %td= expense.category.title
        %td= expense.payment_method.name
        %td
          %span.col-md-6= link_to fa_icon("pencil-square-o", class: "fa-2x"), edit_expense_path(expense)
          %span.col-md-6= link_to fa_icon("trash-o", class: "fa-2x"), expense, method: :delete, data: {confirm: "Are you sure?"}
